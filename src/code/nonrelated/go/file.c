#include "utils.h"

int main()
{
    int t;
    scanf("%d", &t);   

    while(t--)
    {
        int n;
        scanf("%d", &n);
        char str[n + 2];
        fgets(str, n + 2, stdin);
        str[n + 1] = '\0';


    }
    return EXIT_SUCCESS;
}